import { createTheme } from '@material-ui/core/styles';

export const alphaTheme=createTheme({
    palette: {
        primary: {
          main: '#ef5350'
        },
        secondary: {
            main: '#26a69a'
          },
    }
})